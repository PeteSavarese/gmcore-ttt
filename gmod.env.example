# ---------------------------------------------------------------------------
# gmod.env.example — copy to gmod.env and fill in values
# ---------------------------------------------------------------------------

# ---------------------------------------------------------------------------
# Pterodactyl / container internals (do not change)
# ---------------------------------------------------------------------------
SRCDS_APPID=4020
STARTUP=/start-with-healthcheck.sh

# ---------------------------------------------------------------------------
# Deployment
# ---------------------------------------------------------------------------
# local | production
DEPLOY_ENV=local

# Timezone (passed to the container)
TZ=UTC

# ---------------------------------------------------------------------------
# Server identity
# ---------------------------------------------------------------------------
SERVER_ID=1
SERVER_TAG=gmod-ttt
SERVER_NAME=[LOCAL] GMCore TTT
SERVER_LOCATION=us

# ---------------------------------------------------------------------------
# Networking
# ---------------------------------------------------------------------------
PORT=27015
CLIENT_PORT=27005

# ---------------------------------------------------------------------------
# Gameplay
# ---------------------------------------------------------------------------
GAMEMODE=terrortown
MAP=gm_construct
MAX_PLAYERS=16
TICK_RATE=66

# ---------------------------------------------------------------------------
# Steam credentials & tokens
# ---------------------------------------------------------------------------
# Game Server Login Token (https://steamcommunity.com/dev/managegameservers)
GSLT=

# Steam account for SteamCMD auto-update (leave blank for anonymous)
STEAM_USER=
STEAM_PASS=
STEAM_AUTH=

# Steam Web API key (for workshop downloads)
AUTHKEY=

# ---------------------------------------------------------------------------
# Auto-update via SteamCMD on container start
# ---------------------------------------------------------------------------
AUTO_UPDATE=0

# ---------------------------------------------------------------------------
# Logging / development
# ---------------------------------------------------------------------------
LUA_REFRESH=1
LOG_CONSOLE=1

# ---------------------------------------------------------------------------
# Image seeding
# ---------------------------------------------------------------------------
# Controls the rsync-from-image step on each container start.
#  - First boot (no srcds_run_x64 yet): seed always runs regardless of this var.
#    Without it the container has no binary to launch.
#  - Subsequent boots: 1 = skip the rsync, 0 = rsync image content over the
#    container volume. Set to 1 when bind-mounting a local checkout you don't
#    want overwritten.
GMCORE_DISABLE_IMAGE_SEEDING=0

# ---------------------------------------------------------------------------
# Watchdog (local env only — ignored in production)
# ---------------------------------------------------------------------------
# Set to 0 to disable the watchdog when running locally (e.g. while debugging
# before the Lua healthcheck module is loaded). Has no effect outside local env.
WATCHDOG_ENABLED=1

# ---------------------------------------------------------------------------
# Database (MariaDB)
# ---------------------------------------------------------------------------
MARIADB_HOST=mariadb
MARIADB_PORT=3306
MARIADB_USER=user
MARIADB_PASS=
MARIADB_DB_CORE=gmcore_core
MARIADB_DB_FORUMS=gmcore_forums

# ---------------------------------------------------------------------------
# Forums
# ---------------------------------------------------------------------------
FORUMS_BASE_URL=
